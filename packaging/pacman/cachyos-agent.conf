# Personal repository for reviewed CachyOS Agent releases.
# Packages are currently unsigned. HTTPS and the repository database protect
# transport and detect package corruption, but do not replace package signing.
[cachyos-agent]
SigLevel = Optional TrustedOnly
Server = https://raw.githubusercontent.com/keiranhaax/CachyOS-Agent/repo/$arch
